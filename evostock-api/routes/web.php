<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| This backend exposes a JSON REST API only — all application routes live
| in routes/api.php (prefixed with /api/v1). The user-facing entry point is
| the Angular SPA in ../evostock-web. The /up health-check endpoint is
| registered in bootstrap/app.php.
|
*/
